

const wordController = {
    uploadWords: async (req, res) => {
        console.log("uploadWords called");
    },

    getWord: async (req, res) => {
        console.log("getWord called");
    }
};

module.exports = wordController;